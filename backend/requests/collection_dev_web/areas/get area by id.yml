info:
  name: get area by id
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:3000/api/areas/:id
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: form-urlencoded
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
