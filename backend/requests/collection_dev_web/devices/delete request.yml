info:
  name: delete request
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/devices/:id/deletion-request
  params:
    - name: id
      value: "2"
      type: path
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
