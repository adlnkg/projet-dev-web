opencollection: 1.0.0

info:
  name: collection_dev_web

request:
  auth:
    type: bearer
    token: "{{access_token}}"
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
