info:
  name: search info
  type: http

http:
  method: GET
  url: http://localhost:3000/api/search/info
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
