info:
  name: get device by id
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:3000/api/devices/:id
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: form-urlencoded
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
