info:
  name: device search
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:3000/api/devices/search/?keywords&type=ALL
  params:
    - name: keywords
      value: ""
      type: query
    - name: type
      value: ALL
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
